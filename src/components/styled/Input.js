import styled from 'styled-components'

const Input = styled.input`
  background: var(--white);
  border: 0.125rem solid  rgba(134, 110, 48, 0.3);
  border-radius: 1.75rem;
  color: var(--black);
  display: inline-block;
  font-size: 2rem;
  margin: 0.125rem;
  padding: 0.5rem;
  width: 100%;
`

export default Input
