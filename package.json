{
  "name": "ex-post-facto",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently 'npm run server-start' 'npm run client-start'",
    "start-dev": "concurrently 'npm run server-start-dev' 'npm run client-start-dev'",
    "build": "npm run server-build && npm run client-build",
    "server-build": "cd server && npm i --silent",
    "client-build": "cd client && npm i --silent",
    "server-start": "cd server && npm run start",
    "client-start": "cd client && npm run start",
    "server-start-dev": "cd server && npm run server",
    "client-start-dev": "cd client && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jsavage42/ex-post-facto.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jsavage42/ex-post-facto/issues"
  },
  "homepage": "https://github.com/jsavage42/ex-post-facto#readme",
  "devDependencies": {
    "concurrently": "^5.0.2"
  }
}
